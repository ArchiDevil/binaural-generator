<Window x:Class="SubjectUI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SubjectUI"
        mc:Ignorable="d"
        Title="SubjectUI" Height="350" Width="525"
        Loaded="Window_Loaded">
    <Window.Resources>
        <Style TargetType="TextBox">
            <Setter Property="Margin" Value="5"/>
        </Style>

        <Style TargetType="CheckBox">
            <Setter Property="Margin" Value="5"/>
        </Style>

        <Style TargetType="TextBlock">
            <Setter Property="Margin" Value="5"/>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="5*"/>
            <RowDefinition Height="1*"/>
        </Grid.RowDefinitions>

        <Border Margin="5" BorderThickness="1" BorderBrush="#FFABADB3">
            <FlowDocumentScrollViewer x:Name="chatViewer" VerticalScrollBarVisibility="Auto">
                <FlowDocument>
                    <Table x:Name="table">
                        <Table.Columns>
                            <TableColumn Width="50"/>
                            <TableColumn/>
                        </Table.Columns>
                        <TableRowGroup/>
                    </Table>
                </FlowDocument>
            </FlowDocumentScrollViewer>
        </Border>

        <TextBox x:Name="chatType" Grid.Row="4" Padding="5" PreviewKeyDown="TextBox_PreviewKeyDown"/>

        <Grid Grid.Column="1" Grid.RowSpan="2">
            <Grid.RowDefinitions>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
            </Grid.RowDefinitions>

            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>

            <TextBlock Text="Connection status: "/>
            <TextBlock Grid.Column="1" Text="{Binding ConnectionStatus, Mode=OneWay}"/>
            <CheckBox Grid.Row="1" Grid.ColumnSpan="2" IsChecked="{Binding EnableMicrophone}" Content="Enable microphone"/>
            <CheckBox Grid.Row="2" Grid.ColumnSpan="2" IsChecked="{Binding EnableVoice}" Content="Enable experimenter's voice"/>
            <CheckBox Grid.Row="3" Grid.ColumnSpan="2" IsChecked="{Binding EnableSignals}" Content="Enable signals"/>
        </Grid>
    </Grid>
</Window>
